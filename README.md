# app-panel
Простая стартовая страница на Flask, отображающая список приложений с переходами.

Конфигурация `apps.yaml` перечитывается при каждом запросе, поэтому изменения
в файле вступают в силу без перезапуска приложения.
